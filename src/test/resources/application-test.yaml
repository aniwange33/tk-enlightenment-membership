spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
  mail:
    host: localhost
    port: 1025

auth:
  jwt:
    secret: test-secret-key-that-is-definitely-long-enough-for-hs256-signing
    ttl: PT8H
  admin:
    email: admin@taraku-enlightenment.org
    password: AdminTest!2026
